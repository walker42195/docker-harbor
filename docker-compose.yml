services:
  docker-harbor:
    build: .
    container_name: docker-harbor
    restart: always
    ports:
      - "6969:6969"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    env_file:
      - .env
